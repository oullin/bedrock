{
  "name": "@bedrock/str",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "sh -c 'mkdir -p ../../storage/.turbo/dist/str && go build ./... && touch ../../storage/.turbo/dist/str/.build-stamp'",
    "dev": "go test -race ./... -count=1",
    "test": "go test -race ./...",
    "test:coverage": "sh -c 'mkdir -p ../../storage/.cache/coverage/go/str && go test -race ./... -coverprofile=../../storage/.cache/coverage/go/str/coverage.out'",
    "typecheck": "go test ./... -run '^$'",
    "fmt": "gofmt -w .",
    "fmt:check": "sh -c 'test -z \"$(gofmt -l .)\"'"
  }
}
