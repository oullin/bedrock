{
  "name": "@bedrock/authkit",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "sh -c 'mkdir -p ../../storage/.turbo/dist/authkit && go build ./... && touch ../../storage/.turbo/dist/authkit/.build-stamp'",
    "dev": "go test ./... -count=1",
    "test": "go test ./...",
    "test:coverage": "sh -c 'mkdir -p ../../storage/.cache/coverage/go/authkit && go test ./... -coverprofile=../../storage/.cache/coverage/go/authkit/coverage.out'",
    "typecheck": "go test ./... -run '^$'",
    "fmt": "gofmt -w .",
    "fmt:check": "sh -c 'test -z \"$(gofmt -l .)\"'"
  }
}
