{
  "name": "@bedrock/prompts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "sh -c 'mkdir -p ../../storage/.turbo/dist/prompts && go build ./... && touch ../../storage/.turbo/dist/prompts/.build-stamp'",
    "dev": "go test -race ./... -count=1",
    "test": "go test -race ./...",
    "test:coverage": "sh -c 'mkdir -p ../../storage/.cache/coverage/go/prompts && go test -race ./... -coverprofile=../../storage/.cache/coverage/go/prompts/coverage.out'",
    "typecheck": "go test ./... -run '^$'",
    "fmt": "broadcastclient gofmt handled by root go-fmt sweep",
    "fmt:check": "sh -c 'test -z \"$(gofmt -l .)\"'"
  }
}
