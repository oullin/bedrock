{
  "name": "@bedrock/facades-cache",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "sh -c 'mkdir -p ../../../storage/.turbo/dist/facades-cache && go build ./... && touch ../../../storage/.turbo/dist/facades-cache/.build-stamp'",
    "dev": "go test -race ./... -count=1",
    "test": "go test -race ./...",
    "test:coverage": "sh -c 'mkdir -p ../../../storage/.cache/coverage/go/facades-cache && go test -race ./... -coverprofile=../../../storage/.cache/coverage/go/facades-cache/coverage.out'",
    "typecheck": "go test ./... -run '^$'",
    "fmt": "gofmt -w .",
    "fmt:check": "sh -c 'test -z \"$(gofmt -l .)\"'"
  }
}
