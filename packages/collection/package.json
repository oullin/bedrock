{
  "name": "@bedrock/collection",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "sh -c 'mkdir -p ../../storage/.turbo/dist/collection && go build ./... && touch ../../storage/.turbo/dist/collection/.build-stamp'",
    "dev": "go test -race ./... -count=1",
    "test": "go test -race ./...",
    "test:coverage": "sh -c 'mkdir -p ../../storage/.cache/coverage/go/collection && go test -race ./... -coverprofile=../../storage/.cache/coverage/go/collection/coverage.out'",
    "typecheck": "go test ./... -run '^$'",
    "fmt": "echo gofmt handled by root go-fmt sweep",
    "fmt:check": "sh -c 'test -z \"$(gofmt -l .)\"'"
  }
}
