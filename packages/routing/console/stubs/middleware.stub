package {{ namespace }}

// {{ name }} is a routing middleware scaffolded by the console make command.
type {{ name }} struct{}

// Handle is the pipeline entry point for {{ name }}.
//
// The shape (request, next) → result follows bedrock's middleware contract.
// Wrap any pre-processing before the next call, and any post-processing
// around the next call's return value.
func (m *{{ name }}) Handle(request any, next func(any) any) any {
	// Pre-processing here.
	response := next(request)
	// Post-processing here.
	return response
}
