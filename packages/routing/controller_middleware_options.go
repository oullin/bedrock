package routing

// ControllerMiddlewareOptions is a parity alias for [MiddlewareOptions].
//
// The PHP class is named ControllerMiddlewareOptions; the Go shorter name
// reads more naturally inside controller bodies. Both names refer to the
// same underlying type so test ports remain syntactically identical.
type ControllerMiddlewareOptions = MiddlewareOptions
