{
  "name": "@bedrock/framework-broadcasting",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "sh -c 'mkdir -p ../../../storage/.turbo/dist/broadcasting && go build ./... && touch ../../../storage/.turbo/dist/broadcasting/.build-stamp'",
    "dev": "go test ./... -count=1",
    "test": "go test ./...",
    "test:coverage": "sh -c 'mkdir -p ../../../storage/.cache/coverage/go/broadcasting && go test ./... -coverprofile=../../../storage/.cache/coverage/go/broadcasting/coverage.out'",
    "typecheck": "go test ./... -run '^$'",
    "fmt": "gofmt -w .",
    "fmt:check": "sh -c 'test -z \"$(gofmt -l .)\"'"
  }
}
