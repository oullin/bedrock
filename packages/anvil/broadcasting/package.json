{
  "name": "@bedrock/framework-broadcasting",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "sh -c 'mkdir -p ../../../storage/dist/broadcasting && go build ./... && touch ../../../storage/dist/broadcasting/.build-stamp'",
    "dev": "go test ./... -count=1",
    "test": "go test ./...",
    "test:coverage": "sh -c 'mkdir -p ../../../storage/coverage/go/broadcasting && go test ./... -coverprofile=../../../storage/coverage/go/broadcasting/coverage.out'",
    "typecheck": "go test ./... -run '^$'",
    "fmt": "gofmt -w .",
    "fmt:check": "sh -c 'test -z \"$(gofmt -l .)\"'"
  }
}
