{
  "name": "@bedrock/illuminate-reflection",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "sh -c 'mkdir -p ../../../storage/.turbo/dist/reflection && go build ./... && touch ../../../storage/.turbo/dist/reflection/.build-stamp'",
    "dev": "go test ./... -count=1",
    "test": "go test ./...",
    "test:coverage": "sh -c 'mkdir -p ../../../storage/.cache/coverage/go/reflection && go test ./... -coverprofile=../../../storage/.cache/coverage/go/reflection/coverage.out'",
    "typecheck": "go test ./... -run '^$'",
    "fmt": "gofmt -w .",
    "fmt:check": "sh -c 'test -z \"$(gofmt -l .)\"'"
  }
}
